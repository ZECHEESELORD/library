package sh.harold.creative.library.impulse;

public enum ImpulseMode {
    ADD_VELOCITY,
    SET_VELOCITY,
    CLAMPED_ADD,
    DASH_TOWARD_DIRECTION,
    PULL_TOWARD_POINT,
    PUSH_AWAY_FROM_POINT,
    UPWARD_LAUNCH
}
